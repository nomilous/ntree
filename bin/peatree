#!/usr/bin/env node

var peatree = require('../');

peatree.create()

.then(function(tree) {

  console.log(Object.keys(tree._meta.nodes));
  console.log(tree);

})

.catch(function(e) {

  console.log(e.stack);
  process.exit(e.errno || 1);
  
})
